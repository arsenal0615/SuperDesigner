# 布局规律知识库 — 由 UI 美术调整记录沉淀而来
# 每条 pattern 结构：
#   id: string           唯一标识，如 lp_001
#   name: string         规律名称
#   category: layout
#   trigger:             触发条件（component_type、screen_region 等）
#     component_type: Panel
#   apply:               应用的默认属性值
#     gap: 16
#   confidence: high|medium|low
#   usage_count: int
#   disabled: bool       true 时临时禁用
#   source: string       来源记录，如 "UI-张伟 2026-01-15"
patterns: []
